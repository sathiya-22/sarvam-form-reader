# 📋 Sarvam Form Reader
Extract structured data from scanned Indian government forms using Sarvam Vision.

```bash
pip install -r requirements.txt && cp .env.example .env
python reader.py aadhaar.jpg aadhaar output.json
python reader.py form.pdf generic output.json
```
